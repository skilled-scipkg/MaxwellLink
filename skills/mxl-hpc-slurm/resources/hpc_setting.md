# HPC settings at `HPC_PROFILE.json`

The canonical cluster profile is the JSON file at repository/workspace root:

- `HPC_PROFILE.json`

If this file is not available, SLURM is likely not be supported in this machine.